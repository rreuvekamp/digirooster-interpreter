<!DOCTYPE html>
<html lang="en">
<head>
<title>{{.ClassName}}</title>
<link rel="stylesheet" href="style.css" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta charset="utf-8" />
</head>
<body>
<header>
	<div>
		<strong>{{.ClassName}}</strong> - <a href="#">current week</a> - <a href="index.html">home</a> <small>(generated on: {{.GeneratedTimeStr}})</small>
	</div>
</header>
<main>
{{range .Weeks}}
	<h2 id="week_{{.WeekNumber}}">Week: {{.WeekNumber}} ({{.Hours}} hours)</h2>
	<section data-week="{{.WeekNumber}}" data-startat="{{.StartAt}}" data-endat="{{.EndAt}}">
	{{range $index, $d := .Days}}
		<div class="day" data-day="{{$index}}">
		<h3>{{if .DayString}}{{ .DayString}} ({{.Hours}} hours){{else}}&nbsp;{{end}}</h3>
		<div class="activities">
		{{range $d.Activities}}
			<div class="activity{{if .NonImportant}} nonimportant{{end}}{{if .Important}} important{{end}}"  style="margin-top: {{.Padding}}px; min-height: {{.Height}}px; height: {{.Height}}px;">
			<div style="float: right;">{{.StartStr}}</div><h4><span title="{{.OrigDesc}}">{{.Desc}}</span></h4>
			{{.Loc}}<br />
			{{range $i, $s := .Staff}}{{if gt $i 0}}, {{end}}{{$s}}{{end}}<br />
			<div style="float:right;"><span title="{{range $i, $c := .Classes}}{{if gt $i 0}}, {{end}}{{$c}}{{end}}">{{len .Classes}} class{{if gt (len .Classes) 1}}es{{end}}</span></div><small>Ends: {{.EndStr}}</small><br />
			</div>
		{{end}}
		</div>
		</div>
	{{end}}
	</section>
{{end}}
</main>
<script type="text/javascript" src="script.js"></script>
</body>
</html>
